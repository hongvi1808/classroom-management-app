import { Typography } from "@mui/material";
import StudentsPage from "./students/page";

export default function Home() {
  return ( <Typography paragraph>
        Đây là phần nội dung chính, sẽ tự co giãn khi Drawer mở/đóng.
      </Typography>
  );
}
